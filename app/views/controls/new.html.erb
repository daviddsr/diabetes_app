<%= form_for @control do |f| %>
	<%= f.label :level %>
	<%= f.number_field :level %>

	<%= f.datetime_select(:day, default: Time.now + 1.days) %>
	<%= f.select(:period, Control.periods) %>

	<%= f.submit "Save" %>

	<% end %>

